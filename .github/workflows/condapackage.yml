name: conda-env

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    #- uses: actions/checkout@v2
    - uses: goanpeca/setup-miniconda@v1.0.2
      with:
        miniconda-version: 'latest'
    - name: Create new environment tlt
      shell: bash -l {0}
      run: |
        conda env create -n tlt -f ./environment.yml
        conda activate tlt
        conda info --envs
    #- name: Install libGL (for OpenCV)
    #  run: |
    #    sudo apt-get update
    #    sudo apt-get install -y libgl1-mesa-glx #libgl1-mesa-dev
    - name: Install pytest
      run: | 
        conda install pytest
        conda info --envs
        conda list pytest
        which python
    - name: Install pylapsy
      run: python ./setup.py install
    - name: Run pytest
      run: pytest
      
